services:
  app-publish:
    build:
      dockerfile: Dockerfile.publish
      tags:
        - portal-app:test
    logging:
      driver: local
    ports:
      - "8081:8080"
    environment:
      MICRONAUT_ENVIRONMENTS: prod
